# Environment & settings
python-dotenv==1.0.0
pydantic-settings==2.5.2

# LangChain ecosystem (pin aligned minor versions)
langchain==0.3.7
langchain-core==0.3.15
langchain-community==0.3.7
langchain-text-splitters==0.3.2
# If you don’t explicitly need the subpackages above, you can replace them with just:
# langchain==0.3.7

# Providers / integrations
langchain-nvidia-ai-endpoints==0.3.1
langchain-groq==0.2.1          # Verify import; replace underscore version

# Vector DB
qdrant-client==1.11.3

# PDF processing
PyMuPDF==1.24.10

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Networking / HTTP
urllib3==2.2.3
httpx==0.27.2